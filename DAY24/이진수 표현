T = int(input())

for tc in range(1, T+1):

    N, M = map(int, input().split())
    
    # N만큼 비트가 전부 1인 수
    num = (1 << N) -1
    
    # 비트 동일 여부
    if M & num == num:
        print(f'#{tc} ON')
    else:
        print(f'#{tc} OFF')
