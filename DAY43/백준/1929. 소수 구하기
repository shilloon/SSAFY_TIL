#include <iostream>
#include <cmath>

using namespace std;

int main() {

	int N, M;
	cin >> N >> M;

	for (int i = N; i <= M; i++) {

		if (i < 2)
			continue;

		bool is_valid = true;

		for (int j = 2; j <=sqrt(i); j++) {

			if (i % j == 0) {
				is_valid = false;
				break;
			}

		}

		if (is_valid)
			cout << i << "\n";

	}

	return 0;

}
