#include <iostream>
#include <queue>

using namespace std;

int main() {

	int N;
	cin >> N;
	queue<int> q;

	for (int i = 0; i < N; i++) {

		string s;
		cin >> s;

		if (s == "push") {
			int num;
			cin >> num;
			q.push(num);
		}
		else if (s == "empty") {

			if (q.empty())
				cout << 1 << endl;
			else
				cout << 0 << endl;

		}
		else if (s == "pop") {

			if (q.empty())
				cout << -1 << endl;
			else {
				int popnum = q.front();
				q.pop();
				cout << popnum << endl;
			}

		}
		else if (s == "size")
			cout << q.size() << endl;
		else if (s == "front") {
			if (q.empty())
				cout << -1 << endl;
			else
				cout << q.front() << endl;
		}
		else if (s == "back") {
			if (q.empty())
				cout << -1 << endl;
			else
				cout << q.back() << endl;
		}
	}

}
